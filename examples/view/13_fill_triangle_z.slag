// Example: Z-Buffered Triangle
function main() {
    zbuffer.clear();                 // reset depth buffer each frame

    // fill_triangle_z(x0,y0,z0, x1,y1,z1, x2,y2,z2, r,g,b)  lower z = closer
    fill_triangle_z(50,120,100, 200,120,100, 125,250,100, 0,0,255);

    return;
}
